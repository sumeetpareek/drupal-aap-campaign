All feature exports (DB settings/configurations exported via features module) that
need to be added only specific to the DELHI site (production URL =
http://delhi.aamaadmiparty.org/) are to be present here.
